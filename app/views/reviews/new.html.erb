<h1>Add Review</h1>

<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :content %>
  <%= f.text_field :content %> 

  <%= f.label :rating %>
  <%= f.text_field :rating %> 

  <%= f.submit class: "btn btn-primary" %>
<% end %>
